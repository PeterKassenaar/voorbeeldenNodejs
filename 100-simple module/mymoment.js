// myMoment.js - usage of the created library
const mymoment = require('./index');
console.log(mymoment('en', 'Hello World'));

// run this example by typing `node mymoment.js` in the console
// (or use `nodemon mymoment.js`, to restart automatically)
